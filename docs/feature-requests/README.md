# Feature Requests / RFCs

Design proposals for upcoming TinyBooth Sound Studio features. Each RFC follows a consistent shape: header table, Executive summary, Problem, Proposal, Implementation notes, Risks, Open questions, Success criteria. Quote the **Session serial** when asking for revisions to keep continuity with the underlying research.

| ID | Title | Status | Landed in | Notes |
|---|---|---|---|---|
| **TBSS-FR-0001** | [Suno cleanup mode](TBSS-FR-0001-suno-cleanup.md) | ✅ Implemented (DSP) | v0.1.6 | Parametric EQ + de-esser added to FilterChain(Stereo); Suno-Clean preset shipped. Mix-tab path delivered by FR-0002. |
| **TBSS-FR-0002** | [Multitrack remastering](TBSS-FR-0002-multitrack-remastering.md) | ✅ Implemented | v0.2.0 | Mix tab + cpal output player + per-track corrections + A/B bypass + correction-aware export. |
| **TBSS-FR-0003** | [Import normalization](TBSS-FR-0003-import-normalization.md) | 📝 Proposed | — | LUFS-balance imported stems on ingest; non-destructive (gain only). |
| **TBSS-FR-0004** | [Console mixer + volume automation](TBSS-FR-0004-console-mixer-automation.md) | ✅ Implemented | v0.4.x | Hardware-style fader strips on the Mix tab + recordable Catmull-Rom fader automation. |
| **TBSS-FR-0005** | [Track telemetry](TBSS-FR-0005-track-telemetry.md) | ✅ Implemented | v0.4.13–0.4.37 | Pure-DSP analysis (onsets, YIN pitch, key, drum classes, cross-band coherence) + Coherence Restoration filter. |
| **TBSS-FR-0006** | [The machine-learning boundary](TBSS-FR-0006-ml-boundary.md) | ✅ Accepted (policy) | — | Standing policy: no ML in the core analyzer; ML only as a quarantined opt-in sidecar. |
| **TBSS-FR-0007** | [The `.tib` container — single-file SQLite projects with stem revision history](TBSS-FR-0007-tib-container-revisions.md) | ✅ Landed (MVP) | v0.4.42 | Live `.tib` SQLite format: open/play/trim (reversible, FIFO-5)/export/save/hot-swap/telemetry/import-lands-in-.tib + migrate-on-open prompt. Deferred past MVP: stem/revision browser UI + rollback gesture, recording-into-.tib, .tib→folder Save-As. |
| **TBSS-FR-0008** | [Record tab — recordings browser (full directory listing, waveforms, region export) + repeat-take fix](TBSS-FR-0008-record-tab-recordings-browser.md) | 🔧 In progress | v0.4.43 (items 1/2/4 + 3-partial) | Items 1/2/4 shipped: Loose WAVs section, path-label 📋/📂 buttons, per-take waveform thumbnail + click-drag region selection + 💾 Export Selection (lossless crop via `trim::crop_wav_bytes`). Item 3 partial — orphan-WAV cleanup shipped; the underlying cpal repeat-take race still needs a real repro signal to fix at root. Loose-WAV adopt-into-manifest + ▶/🗑 parity deferred. |
| **TBSS-FR-0009** | [Generator track — binaural / isochronic / layered focus music as a synthesized stem](TBSS-FR-0009-generator-track.md) | ✅ Landed (MVP) | v0.4.43 | New `TrackSource::Generator { mode, last_bake_at, last_bake_master_signature }` variant with `Track::is_locked()`; on-demand bake (`File → Add Generator Track…`) into the existing audio path (`.tib` revision or folder WAV) plus a timestamped export under `exports/generator-bakes/`. Binaural + Isochronic DSP shipped; Layered is a disabled architectural slot. **Reading A** confirmed for "meld with master chain" — bake snapshots a `MasterSignature` for dirty detection only; playback routes through master like every other track. Per-lane dirty indicator + edit-params affordance deferred. |
| **TBSS-FR-0010** | [Crossfade tab — two-track preview + export](TBSS-FR-0010-crossfade-tab.md) | 📝 Proposed | — | Dedicated Crossfade tab. Load any two WAVs from disk, slider for track-B start offset, equal-power (sin²/cos²) crossfade across the overlap, visual waveforms with overlap highlighted. ▶ A / ▶ B / ▶ Crossfade / ■ Stop on a dedicated `CrossfadePreviewSession` (own cpal stream). Export to any format the existing `export.rs` supports (WAV / FLAC / MP3 / Ogg / Opus / M4A). Doesn't depend on a loaded project. Drag-on-waveform timing + linear-curve picker deferred. |
| **TBSS-FR-0011** | [`.tib` as buffered stem (mix-run cache) + Crossfade-of-.tib](TBSS-FR-0011-tib-stem-cache.md) | ✅ Landed (MVP) | v0.4.50 | `.tib` schema v2 adds a single-row `mix_run` table holding a buffered render of the project's master mix (WAV blob + sample_rate/channels/frames + source signature). Mix-tab `⤓ Bounce` button stamps it; Crossfade tab Load… now accepts `.tib` as a single stem (reads the blob) so finished projects compose without manual .wav round-trips. Auto-bounce-on-export + multi-slot cache deferred. |
| **TBSS-FR-0013** | [Chord-chart video generator](TBSS-FR-0013-chord-video.md) | ✅ Implemented | v0.4.74 | New "Chords" tab: song in → same untouched audio + a synced fretboard-diagram video track (one chord at a time, EADGBE). **All five epics landed** — E1 `chordgrid.rs` (STFT→onset→tempo→beat grid→beat-sync chroma→template match→verb), E2 `ui/chordvideo.rs` (editable grid, confidence flags, background jobs), E3 `chordvoice.rs` (spans + fret-travel-minimising voicing), E4 `fretboard.rs` (pure rasteriser → `RgbaImage`, one renderer for both egui and ffmpeg), E5 `chordvideo.rs` (concat-demuxer mux, probed H.264 encoder, stream-copied audio). Underneath: `chorddb.rs`, a **generative fret-matrix engine** producing **2,287 pitch-class-verified voicings** across 16 qualities × 12 roots, with the 44 hand-verified shapes as its golden set. End-to-end 10s slice proven as a test. GitHub epic #2 / sub-issues #3–#7. Also fixed a pitch-class offset bug in the shipped tbviz chroma labels. |
| **TBSS-FR-0012** | [TinyBooth Album (`.tba`)](TBSS-FR-0012-tinybooth-album.md) | 🔧 In progress (MVP) | v0.4.52 | New sibling project format alongside `.tib` — a `.tba` is a SQLite container holding `clips` (each referencing an external `.tib` by path with `start_secs`, `fade_in_secs`, `fade_out_secs`, `gain_db`) plus its own `mix_run` table for the bounced album master. New Album tab: clip-list editor, top-down timeline visualization, ▶ Preview / ⤓ Bounce / Export, Open / Save / Save As. Render DSP: per-clip equal-power fades + linear gain summed onto a single timeline; emergent crossfade where adjacent fades overlap. The bounced album's `mix_run` is the same shape as `.tib`'s, so albums can themselves be loaded as stems (Crossfade or another Album) — recursive composition. Timeline drag editing + `.wav`-as-clip-source + relative source paths deferred to v0.4.53. |

| **TBSS-FR-0014** | [Retro tracker / loop sampler](TBSS-FR-0014-retro-tracker.md) | 📝 Proposed | — | New Tracker tab in the ProTracker lineage: step grid (rows = time, columns = instrument lanes), each lane bound to a user-configured sample (recording take, `.tib` stem, or file), per-step pitch (authentic variable-rate playback) + velocity, swing, loop transport, ⤓ Bake-as-stem (FR-0009 path) + WAV export. E1 model/render → E2 loop transport → E3 tab UI → E4 sinks. |

| **TBSS-FR-0015** | [Record tab: in-listing playback + live monitor](TBSS-FR-0015-record-tab-live-monitor-and-listing-playback.md) | ✅ Implemented | v0.4.81 | Takes audition in place: ▶/■ per row, scrubbable playhead on the thumbnail, app-wide audio exclusivity, ⇪ integrate-into-project (folder + `.tib`), ▶-to-mixer detour retired. 🎙 live input monitor: capture stream + DSP chain + viz with no WAV writer, for base levels before recording. |

## Convention

- File name: `TBSS-FR-NNNN-kebab-case-title.md`. Numbers monotonic, never reused.
- Status values: `📝 Proposed`, `🔧 In progress`, `✅ Implemented`, `⛔ Withdrawn`.
- Once an RFC is implemented, the doc stays in this folder as the historical record. The header table's "Status" line is updated; the body is left as-was so the design rationale at decision time is preserved.
- A revision (e.g. FR-0001's §7 was rewritten when Suno's server-side stems made local separation obsolete) is annotated inline rather than splitting into a new RFC, unless the revision is large enough to warrant its own document.
